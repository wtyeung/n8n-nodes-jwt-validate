export declare const enum NodeDiffStatus {
    Eq = "equal",
    Modified = "modified",
    Added = "added",
    Deleted = "deleted"
}
export type NodeDiff<T> = {
    status: NodeDiffStatus;
    node: T;
};
export type WorkflowDiff<T> = Map<string, NodeDiff<T>>;
export declare function compareNodes<T extends {
    id: string;
}>(base: T | undefined, target: T | undefined): boolean;
export declare function compareWorkflowsNodes<T extends {
    id: string;
}>(base: T[], target: T[], nodesEqual?: (base: T | undefined, target: T | undefined) => boolean): WorkflowDiff<T>;
//# sourceMappingURL=workflow-diff.d.ts.map